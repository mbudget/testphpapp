# A stupid, simple and ugly test for packaging YunoHost applications

## Installation

Read schema.sql into your MySQL database, then adapt config.php to point at whatever database you loaded the schema into.

Done!
